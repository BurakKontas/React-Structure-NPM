{
  "name": "react-structure-generator",
  "version": "1.0.1",
  "description": "This npm package is used to facilitate the creation of configurations for React applications. It utilizes ready-made templates to create pages, components, Redux stores, and other structures, organizing them according to the specified names.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "link": "npm run build && npm link"
  },
  "bin": {
    "react-structure-generator": "./dist/index.js"
  },
  "author": "BurakKontas",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist"
  ]
}
